stages:
    - build

variables:
    JAVA_VERSION: "11"

#
# stage: build tasks
#
build_backend:
    stage: build
    before_script:
        - . envcfg
        - envcfg -p
    script:
        - mvn verify