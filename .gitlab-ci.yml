stages:
    - env
    - build

variables:
    JAVA_VERSION: "10"

#
# stage: env tasks
#
env_config:
    stage: env
    script:
        - envcfg -r
        - envcfg -p

#
# stage: build tasks
#
build_backend:
    stage: build
    script: mvn verify